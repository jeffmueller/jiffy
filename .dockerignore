# Reinstalled/rebuilt inside the image
node_modules
.next
out
build
*.tsbuildinfo

# Secrets — API keys are supplied at runtime, never baked into the image
.env
.env.*
!.env.example

# Version control and editor/agent tooling
.git
.gitignore
.claude
.skills

# Not part of the web app
JiffyiMessage
deployment

# Docker's own files
Dockerfile
.dockerignore
docker-compose.yml

# Docs and cruft
README.md
AGENTS.md
CLAUDE.md
.DS_Store
**/.DS_Store
*.pem
npm-debug.log*
